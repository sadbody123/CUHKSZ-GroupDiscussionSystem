# Speech delivery proxy metrics (phase 10)

This subsystem estimates **pace, pauses, fillers, and energy proxies** from WAV PCM and transcript text. Outputs are **explicitly labeled as proxy / heuristic** and must not be interpreted as pronunciation test scores.

See also: `pronunciation_proxy_limitations.md`.
