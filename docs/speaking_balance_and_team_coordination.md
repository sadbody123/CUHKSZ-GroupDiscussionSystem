# Speaking balance and team coordination

`speaking_balance.py` aggregates per-participant and per-team transcript length proxies (character counts — not real speaking time). `group_report_builder.py` emits flags such as dominant speaker or low user participation.

Team coordination hints are short deterministic strings used in prompts (`team_coordination.py`).
